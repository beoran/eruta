#include "gy.h"

#define GY_INTERN_ONLY
#include "gy_intern.h"

/*
struct GySprite_ {
  int         x, y, z; 
  // these are "world" coordinates, that is, related to the layer they are in.
  GyMovie  * frame;
  // The movie will be drawn and can be used for animation
};


GySprite * gysprite_image_(GySprite * sprite, GyImage * image) {
  return sprite;
}

GySprite * gysprite_init(GySprit * sprite, int x, int y, int z) {
  if(!sprite) return NULL;
  sprite->x = x;
  sprite->y = y;
  sprite->z = z;
  // gymovie_image_(sprite->movie, NULL);  
  return sprite;
}
*/




