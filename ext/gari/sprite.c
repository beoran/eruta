#include "gari.h"

#define GARI_INTERN_ONLY
#include "gari_intern.h"

/*
struct GariSprite_ {
  int         x, y, z; 
  // these are "world" coordinates, that is, related to the layer they are in.
  GariMovie  * frame;
  // The movie will be drawn and can be used for animation
};


GariSprite * gari_sprite_image_(GariSprite * sprite, GariImage * image) {
  return sprite;
}

GariSprite * gari_sprite_init(GariSprit * sprite, int x, int y, int z) {
  if(!sprite) return NULL;
  sprite->x = x;
  sprite->y = y;
  sprite->z = z;
  // gari_movie_image_(sprite->movie, NULL);  
  return sprite;
}
*/




